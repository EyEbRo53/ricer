# Project Naming Rules

**Rule 1:**

- All tool and script file names must begin with `set` (e.g., `set_wallpaper.py`, not `change_wallpaper.py`).
- All resource file names must begin with `get` (e.g., `get_wallpaper.py`).
- This rule applies project-wide.
